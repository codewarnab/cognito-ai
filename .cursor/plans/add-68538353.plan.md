<!-- 68538353-fb8d-45b0-b902-d40b81f07537 72282fad-0e4d-4d60-975f-dd3db912e9c9 -->
# Inter Font Integration Plan

## Phase 1 – Gather Assets & Configure Manifest ✅

- ✅ Audited existing `public/fonts/static/` and kept only required Inter weights (Regular 400, Medium 500, SemiBold 600).
- ✅ Moved retained font files to `src/assets/fonts/` with clean naming.
- ✅ Identified target UI surfaces (`src/style.css`, `src/sidepanel.css`, `src/styles/index.css`, base components).
- ✅ Declared font files in `package.json` manifest `web_accessible_resources`.

## Phase 2 – Define Font-Face & Fallbacks ✅

- ✅ Created `src/styles/fonts.css` with `@font-face` blocks for Regular, Medium, and SemiBold weights.
- ✅ Used `chrome-extension://__MSG_@@extension_id__/` paths for proper extension loading.
- ✅ Added sensible fallbacks: `'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif`.

## Phase 3 – Apply Typography ✅

- ✅ Imported fonts.css in `src/style.css`, `src/sidepanel.css`, and `src/styles/index.css`.
- ✅ Updated font-family declarations in `src/style.css` body and `src/styles/core/base.css` sidepanel-container.
- ✅ All changes pass diagnostics with no errors.

## Verification & QA

- ⏳ Re-run build to confirm assets bundle correctly.
- ⏳ Manually test extension views to confirm Inter loads (check Network tab for font files).
- ⏳ Consider Lighthouse or UX checks for readability and performance impacts.

### Implementation Summary

**Files Created:**
- `src/assets/fonts/Inter-Regular.ttf`
- `src/assets/fonts/Inter-Medium.ttf`
- `src/assets/fonts/Inter-SemiBold.ttf`
- `src/styles/fonts.css`

**Files Modified:**
- `package.json` - Added font files to web_accessible_resources
- `src/style.css` - Imported fonts.css and updated body font-family
- `src/styles/index.css` - Imported fonts.css
- `src/sidepanel.css` - Imported fonts.css
- `src/styles/core/base.css` - Updated sidepanel-container font-family

### To-dos

- [x] Decide Inter weights and loading method
- [x] Add font import/@font-face and apply globally
- [ ] Test rendering in dev build
- [ ] Verify no CSP or loading errors in DevTools