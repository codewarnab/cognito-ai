/**
 * Workflow Hooks
 *
 * Hooks for workflow-related functionality including:
 * - Slash command workflow selection
 * - Workflow mode management
 *
 * @example
 * import { useWorkflowMode } from '@/hooks/workflows';
 */

export { useWorkflowMode } from './useWorkflowMode';
