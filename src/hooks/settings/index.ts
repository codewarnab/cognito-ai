/**
 * Settings Hooks
 *
 * Hooks for settings and configuration management including:
 * - API key management
 * - Onboarding state and persistence
 * - Logger configuration
 *
 * @example
 * import { useApiKey, useOnboarding, useLoggerConfig } from '@/hooks/settings';
 */

export { useApiKey } from './useApiKey';
export { useOnboarding } from './useOnboarding';
export { useLoggerConfig } from './useLoggerConfig';
