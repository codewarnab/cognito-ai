import React from "react";

/**
 * ActionRenderer
 *
 * Purpose: A placeholder component intended to host shared UI for
 * rendering local Frontend Tool executions, if we decide to centralize
 * them in one place. For now, each tool provides its own `render`
 * function, so this returns null.
 */
export default function ActionRenderer(): React.ReactElement | null {
    return null;
}


