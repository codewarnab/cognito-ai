// Re-export commonly used types from chat feature
export type { Message, FileAttachment, AIMode, RemoteModelType, ModelState } from '../features/chat/types';

// Add other shared types here as needed
